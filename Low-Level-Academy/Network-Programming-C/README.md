# Network-Programming-C

This repo is now split by topic:

- `http/` contains the HTTP experiments.
- `websocket/` contains the separate socket-based client/server experiment.

Note: the `websocket/` code is not a full RFC 6455 WebSocket implementation yet. It is currently a custom TCP protocol prototype that you can evolve into a real WebSocket server/client.
